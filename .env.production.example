# Production Environment Variables for Vercel Deployment
# Copy these to your Vercel environment variables dashboard

NODE_ENV=production

# Frontend URL (your deployed Vercel URL)
FRONTEND_URL=https://your-app-name.vercel.app

# Database (REQUIRED - Get from MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/agripulse_prod

# JWT Security (REQUIRED - Generate a strong secret)
JWT_SECRET=your_super_secure_jwt_secret_key_for_production_change_this

# Paystack Payment (REQUIRED - Get from Paystack dashboard)
PAYSTACK_SECRET_KEY=sk_live_your_paystack_live_secret_key
PAYSTACK_PUBLIC_KEY=pk_live_your_paystack_live_public_key

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@domain.com
EMAIL_PASS=your-app-password

# Data Processing
DATA_ANONYMIZATION_SALT=your_data_anonymization_salt_for_production
DATA_QUALITY_THRESHOLD=0.8